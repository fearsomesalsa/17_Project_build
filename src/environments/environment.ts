export const environment = {
  production: true, // добавила свойство вручную, т.к. angular cli генерирует пустой объект. тут значения наоборот, т.к. второй файл environment.development.ts, значит этот прод
  apiURL: 'https://testologia.site/', // указывается домен, слэш
};
